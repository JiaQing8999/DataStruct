To run the Course Management Subsystem, run the courseDriver,java at the control package. 

To run the Tutorial Group Subsystem, run the TutorialGroupDriver.java at the control package.

To run the Programme Management subsystem, run the ProgrammeManagementSubsystem.java at the control package.

To run the Tutor Management Subsystem, run ManageTutor.java at the control package.
